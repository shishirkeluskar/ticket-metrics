spring:
  application:
    name: ticket-metrics
  datasource:
    url: jdbc:sqlite:src/test/resources/sqllite-db/test.db
    driver-class-name: org.sqlite.JDBC

grpc:
  server:
    port: 0  # Use dynamic port in tests

logging:
  level:
    com.shishir.ticketmetrics: DEBUG